apiVersion: v1
kind: Service
metadata:
  name: gaming-website-service
  namespace: gaming-website
  labels:
    app: gaming-website
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: gaming-website
